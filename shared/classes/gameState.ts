import DefaultGameState from '../constants/defaultGameState';

export default class GameState {
  health = DefaultGameState.health;
  position = DefaultGameState.position;
  magic = DefaultGameState.magic;
  deaths = DefaultGameState.deaths;
  rupees = DefaultGameState.rupees;
  frozen = DefaultGameState.frozen;
  inventory = DefaultGameState.inventory;
  songs = DefaultGameState.songs;
  medallions = DefaultGameState.medallions;
  stones = DefaultGameState.stones;
  equipment = DefaultGameState.equipment;
  progressives = DefaultGameState.progressives;
  counts = DefaultGameState.counts;
  other = DefaultGameState.other;
}
